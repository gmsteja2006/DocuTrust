# DocuTrust - Enterprise Advanced RAG Platform (Vercel Deployment)
# NOTE: Vercel serverless has a 250MB function size limit.
# Heavy ML packages (torch, sentence-transformers) are excluded.
# The grader falls back to a lightweight cosine-similarity approach on Vercel.

# Core Framework
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0
pydantic-settings>=2.0.0

# MongoDB
motor>=3.3.0
pymongo>=4.6.0

# LLM (lightweight, API-based — no torch needed)
google-generativeai>=0.3.0

# PDF Processing (pure Python, lightweight)
pypdf>=3.17.0

# LangGraph (lightweight graph engine)
langgraph>=0.2.0
langchain-core>=0.1.0

# Web Search Fallback
duckduckgo-search>=5.0

# Utilities
python-dotenv>=1.0.0
sse-starlette>=2.1.0
numpy
